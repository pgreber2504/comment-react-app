import React from 'react';
import classes from 'LoadSpinner.module.css'

const LoadSpinner = () => {
    return (
        <div className={classes['load-spinner']}></div>
    )
}

export default LoadSpinner